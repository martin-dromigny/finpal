<!-- app/templates/components/transactions-item.hbs -->
<div class="panel panel-default transactions-item">
    <div class="panel-heading">
        <h3 class="panel-title">{{item.name}}</h3>
    </div>
    <div class="panel-body">
        <p>Address: {{item.created_at}}</p>
        <p>Phone: {{item.counterparty_name}}</p>
        <p>Phone: {{item.operation_type}}</p>
        <p>Phone: {{item.amount}}</p>

        currency
        attachements
        debit
        credits
    </div>
    <div class="panel-footer text-right">
      {{yield}}
    </div>
</div>
